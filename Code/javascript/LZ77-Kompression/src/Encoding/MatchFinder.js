import { Dictionary } from "./Dictionary.js";

class MatchFinder {

    lookForMatch(string, searchBufferLength, lookaheadBufferLength) {
        let dictionary = new Dictionary();

        let i = 0;
        while (i < string.length) {
            
        }
    }
}